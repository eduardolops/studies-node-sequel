extends ./../layout/main

block content
    h1 #{msg}
    ul
      li #{event.id}
      li #{event.name}
      li #{event.description}
    
    a(href="/events/", class="btn btn-secondary btn-sm") Back to the list